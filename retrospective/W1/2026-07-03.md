# 1일 회고 - 2026-07-03

## 1. 리뷰 (사실 정리 & 평가)

#### 오늘의 한 일
- mission3 코드 점검
- mission3에서 테이블에 없는 Region 컬럼을 추출하고 있던 문제를 발견하고, 국가-대륙 매핑 라이브러리를 이용해 해결

#### 오늘의 핵심 성과
- mission3의 Region 파싱 로직을 실제 테이블 구조에 맞게 전면 수정

#### 오늘의 핵심 이슈
- 크롤링 대상 Wikipedia 테이블에는 애초에 Region 컬럼이 없었는데, 잘못된 컬럼 인덱스로 인해 IMF 값을 Region으로 잘못 읽어오고 있었음
- 처음엔 `pycountry_convert` 라이브러리로 국가명 → 대륙 변환을 시도했으나, 위키피디아 표기와 라이브러리 내부 국가명이 일치하지 않아 일부 국가(DR Congo, Kosovo 등)에서 매칭 실패
- `country_converter`(coco) 라이브러리로 교체해 표기 차이에도 안정적으로 매칭되도록 해결

---

## 2. 회고 (KPT — 과정 개선)

#### Keep (잘해서 계속 유지할 행동)
- 제출한 코드를 다시 한 번 처음부터 꼼꼼히 점검한 것 — 덕분에 Region뿐 아니라 GDP 컬럼 인덱스 오류까지 함께 발견

#### Problem (문제가 발생한 행동)
- 어제 작성한 코드를 어제 바로 리뷰하지 않아서, 오늘에서야 문제를 발견하게 된 것

#### Try (다음에 새롭게 시도할 행동)
- 코드 작성 당일과 다음 날, 두 번에 걸쳐 코드를 점검하는 습관 들이기