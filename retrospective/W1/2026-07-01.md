# 1일 회고 - 2026-07-01

### 1. 리뷰 (사실 정리 & 평가)

#### 오늘의 한 일
- 1주차 mission1 수행 (mtcars 데이터셋 분석)

#### 오늘의 핵심 성과
- pandas의 기본 탐색 메서드(head/tail/shape/info/describe)를 한 세트로 실습해 데이터 구조를 빠르게 파악하는 흐름을 익힘
- `groupby`/`crosstab`으로 범주형 변수 조합별 집계를 하고, 이를 matplotlib 바 차트로 옮기는 과정을 연습함
- 상관계수 행렬을 통해 변수 간 관계를 수치로 확인하고, 그중 의미 있는 조합(cyl-disp, cyl-hp 등)을 골라 산점도로 검증하는 분석 흐름을 경험함

#### 오늘의 핵심 이슈
- 바 차트 x축을 `df1.index`로 그려서 실제 (gear, am) 조합 라벨이 아니라 단순 정수 인덱스로 표시됨 → 라벨링 개선 필요

---

### 2. 회고 (KPT — 과정 개선)

#### Keep (잘해서 계속 유지할 행동)
- 미션에서 요구한 순서(탐색 → 집계 → 시각화 → 상관관계)를 그대로 따라가며 논리적으로 분석을 진행한 점
- LLM에 의존하지 않고 구글 검색을 해가며 스스로 코드를 작성한 점

#### Problem (문제가 발생한 행동)
- 바 차트의 x축 라벨링을 신경 쓰지 않아 그래프만 봐서는 어떤 조합인지 알기 어려움

#### Try (다음에 새롭게 시도할 행동)
- 바 차트 등 범주형 조합 시각화 시 `xticks`로 실제 라벨을 명시적으로 지정하기
- `df.corr()` 결과를 히트맵(seaborn 등)으로도 시각화해 상관관계를 더 직관적으로 파악해보기