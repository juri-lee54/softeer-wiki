# 목차
- [목차](#목차)
- [미션 설명](#미션-설명)
- [W3M3/DE-20260713과 겹치는 개념](#w3m3de-20260713과-겹치는-개념)
- [새로 배운 핵심 개념](#새로-배운-핵심-개념)
  - [1. `csv` 모듈로 정형 텍스트 파싱하기](#1-csv-모듈로-정형-텍스트-파싱하기)
  - [2. Hadoop Streaming에서 non-ASCII 인코딩 다루기](#2-hadoop-streaming에서-non-ascii-인코딩-다루기)
  - [3. 설계 결정 — 키워드 사전으로 직접 분류](#3-설계-결정--키워드-사전으로-직접-분류)
- [트러블슈팅](#트러블슈팅)
  - [`csv.reader`가 반환하는 게 행이 아니라 iterator였던 실수](#csvreader가-반환하는-게-행이-아니라-iterator였던-실수)
  - [바깥 리스트와 그 안의 행을 헷갈린 인덱싱 실수](#바깥-리스트와-그-안의-행을-헷갈린-인덱싱-실수)
  - [단어 단위로 print해서 카운트가 로직](#단어-단위로-print해서-카운트가-로직)
  - [`except OSError`가 사실 아무것도 못 잡고 있었던 문제](#except-oserror가-사실-아무것도-못-잡고-있었던-문제)

<br>
<br>

# 미션 설명

Sentiment140 트위터 데이터셋(160만 건)을 Hadoop Streaming + Python으로 처리해, 트윗을 positive/negative/neutral로 분류하고 범주별 개수를 집계하는 미션. w3m3(단어 수 세기)와 클러스터·제출 방식은 동일하고, **정형(CSV) 입력을 다루는 것과 분류 로직 설계**가 이번 미션에서 새로 추가된 부분.

<br>

# W3M3/DE-20260713과 겹치는 개념

아래는 이미 정리돼 있어서 생략/요약만 한다.

- Hadoop Streaming의 stdin/stdout 기반 동작, `-file`/`-combiner`/split 옵션 등 job 제출 방식 → [w3m3.md](./w3m3.md)
- Streaming Reducer가 그룹핑을 직접 구현해야 한다는 것(같은 키 누적 → 키 바뀌면 flush → 마지막 키도 루프 후 flush) → [w3m3.md](./w3m3.md) 4절
- Map/Reduce/Combiner/Partitioner 등 MapReduce 이론 전반, `sentiment\t1` 포맷의 이유 → [DE-20260713.md](./DE-20260713.md) MapReduce 절
- 로컬 파이프라인(`mapper | sort | reducer`)으로 Hadoop 제출 전에 먼저 검증하는 습관 → [w3m3.md](./w3m3.md) 7절
- `BrokenPipeError`가 `head`로 자를 때 나는 정상 현상이라는 것 → [w3m3.md](./w3m3.md)

<br>

# 새로 배운 핵심 개념

## 1. `csv` 모듈로 정형 텍스트 파싱하기 

w3m3는 순수 텍스트(전자책)라 한 줄 = 한 문장이었지만, 이번 데이터는 `"polarity","id","date","query","user","text"` 형태의 **CSV 한 줄 = 레코드 하나**였다. 트윗 본문(`text`) 안에 콤마가 그대로 들어있을 수 있어서(`"Hello, world!"` 같은 트윗), `line.split(',')`로 단순히 쪼개면 필드 개수가 틀어진다. 이걸 해결하는 게 표준 라이브러리 `csv` 모듈.

```python
import csv
row = list(csv.reader([line]))[0]   # ['0', '1467810369', 'Mon Apr 06 ...', 'NO_QUERY', '_TheSpecialOne_', "@switchfoot ... bummer."]
```

- `csv.reader`는 따옴표(`"`)로 감싸진 필드 내부의 콤마는 구분자로 취급하지 않고, 필드 하나로 묶어서 돌려준다. 정규식이나 수작업 split으로 이걸 직접 구현하려면 훨씬 번거롭다.
- `csv.reader(iterable)`는 **"한 줄짜리 문자열"이 아니라 "줄들을 순회할 수 있는 무언가"**를 인자로 받는다. 파일 객체(`open(...)`)를 통째로 넘기는 게 일반적인 쓰임이고, Streaming처럼 한 줄씩 처리해야 할 땐 그 한 줄을 리스트에 담아 `csv.reader([line])`처럼 넘겨야 한다.
- 이 데이터셋은 **헤더가 없는 CSV**라서 필드에 이름으로 접근하는 `csv.DictReader`는 못 쓰고, 인덱스(`row[5]`)로만 접근 가능.
- 파싱이 깨질 때 나는 예외는 `csv.Error`.

<br>

## 2. Hadoop Streaming에서 non-ASCII 인코딩 다루기

Sentiment140은 `latin-1`로 인코딩돼 있는데, Python3의 `sys.stdin`은 기본적으로 UTF-8로 디코딩을 시도한다. 그대로 두면 특정 문자에서 `UnicodeDecodeError`가 날 수 있어서, stdin 자체를 다른 인코딩으로 다시 감싸야 한다.

```python
sys.stdin = io.TextIOWrapper(sys.stdin.buffer, encoding="latin1")
```

`sys.stdin.buffer`는 디코딩되지 않은 raw 바이트 스트림이고, 그걸 원하는 인코딩으로 다시 `TextIOWrapper`로 감싸는 방식. **`run()` 안에서 루프 시작 전 한 번만** 해줘야 하고(줄마다 하면 안 됨), `sys.stdin`이 아니라 `sys.stdout.buffer`를 감싸면 당연히 아무 효과가 없다(트러블슈팅 참고).

<br>


## 3. 설계 결정 — 키워드 사전으로 직접 분류

Sentiment140은 원래 `polarity` 컬럼(0/2/4)에 이미 감정 라벨이 있는 데이터셋이지만, 과제 요구사항이 "미리 정의된 키워드를 기반으로 분류"였기 때문에 **기존 라벨을 쓰지 않고** `POSITIVE_WORDS`/`NEGATIVE_WORDS` 키워드 집합과 텍스트를 직접 비교하는 방식을 선택했다.

- 트윗을 토큰화한 뒤, 두 집합에 각각 몇 개나 매칭되는지 센다.
- positive 매칭 수 > negative 매칭 수 → `positive`, 반대면 `negative`, **동점(둘 다 0인 경우 포함)이면 `neutral`**로 확정.
- 이 tie-break 규칙을 명확히 정해두지 않으면 나중에 "왜 이 트윗이 neutral로 빠졌지?"를 설명할 수 없다.
- 참고로 이 방식대로 하면 사전에 없는 평범한 표현의 트윗은 전부 `neutral`로 몰리므로(실제 결과: neutral 964,679 > positive 421,539 > negative 213,782), neutral 비중이 압도적으로 높게 나오는 게 설계상 자연스러운 결과다.

<br>

# 트러블슈팅

## `csv.reader`가 반환하는 게 행이 아니라 iterator였던 실수

```python
row = csv.reader([line])
return row[4]          # TypeError: '_csv.reader' object is not subscriptable
```

`csv.reader(...)`는 값을 바로 담고 있는 리스트가 아니라 **필요할 때 한 행씩 꺼내주는 iterator**다. `for`로 순회하거나 `next()`/`list()`로 강제로 꺼내야 실제 행(필드 리스트)이 된다. 인덱싱은 그 다음에나 가능.

<br>


## 바깥 리스트와 그 안의 행을 헷갈린 인덱싱 실수

`list()`로 감싼 뒤에도 한 단계를 더 헷갈렸다.

```python
row = list(csv.reader([line]))   # [[f0, f1, f2, f3, f4, f5]]  ← 행이 1개 들어있는 "바깥 리스트"
return row[-1]                   # 바깥 리스트의 마지막 원소 = 행 전체(리스트) — text 필드가 아니다!
```

`row`는 "행 하나짜리 리스트"이지 "필드들의 리스트"가 아니다. 행 자체를 먼저 꺼낸 뒤(`row[0]`), 그 안에서 필드를 인덱싱(`[5]`)해야 한다 → `row[0][5]`. 두 단계(바깥=행들의 목록, 안쪽=한 행의 필드들)를 명확히 구분하는 게 핵심.

<br>

## 단어 단위로 print해서 카운트가 로직 

```python
for word in self.tokenize(line):
    if word in POSITIVE_WORDS:
        print("positive\t1")   # 트윗 하나에 긍정 단어가 3개면 3번 출력됨
```

트윗 1개는 카운트 1개여야 하는데, 매칭되는 단어가 나올 때마다 매번 print하면 카운트가 단어 수만큼 부풀려진다. 트윗 전체를 다 훑어서 positive/negative 매칭 수를 각각 센 다음, **트윗당 딱 한 번만** 카테고리를 결정하고 출력하도록 고쳤다.

<br>

## `except OSError`가 사실 아무것도 못 잡고 있었던 문제

빈 줄이나 깨진 CSV 행을 방어하려고 넣었던 `except OSError`는 실제로 저 상황에서 발생하는 예외 타입이 아니었다(`OSError`는 파일/디스크 I/O 문제일 때 나는 예외). 실제로 필요했던 건 `csv.Error`(파싱 실패)와 `IndexError`(필드 수 부족)였다. "일단 뭔가 잡아두면 안전하겠지"로 예외 타입을 추측하지 말고, **실제로 어떤 상황에서 어떤 예외가 나는지 먼저 정의한 뒤** except 절을 써야 한다.
